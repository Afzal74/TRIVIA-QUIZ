/**
 * Loading Component
 * 
 * A minimal loading component that returns null.
 * This is used by Next.js for loading states between page transitions.
 * Currently empty as the application uses custom loading states.
 */
export default function Loading() {
  return null
}
